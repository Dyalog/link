This function allows you to remove an item from the link, removing its representation in the linked directory. Use this function instead of `⎕EX` or `)ERASE` to maintain synchronisation.

#### Arguments

- items

#### Result