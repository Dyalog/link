 ok←test_shortargs(folder name);here;simple;spaces;bits;hereref
⍝ Verify that short argument handling works

 here←⍕hereref←⎕THIS
 simple←'/myapp'
 bits←1↓¨' '(=⊂⊢)' ',spaces←'/my app'

 ⍝ Unit test for preProcessNsDir
 ⍝   returns (container targetns directory)

 assert 'hereref here simple≡⎕SE.Link.preProcessNsDir simple'
 assert 'hereref here simple≡⎕SE.Link.preProcessNsDir ⊂simple'
 assert 'hereref here simple≡⎕SE.Link.preProcessNsDir here simple'

 assert 'hereref here spaces≡⎕SE.Link.preProcessNsDir spaces'
 assert 'hereref here spaces≡⎕SE.Link.preProcessNsDir ⊂spaces'
 assert 'hereref here spaces≡1 ⎕SE.Link.preProcessNsDir (⊂here),bits'

 ok←1
 ⍝ Add at least one real test for Create, Import and Export with no namespace specified
