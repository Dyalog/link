 items←GetItemName files;names;ns;⎕IO
 ⍝ Get APL names from file names
 ⍝ returns empty string for unlinked files
 :If U.debug=2
     (1+⊃⎕LC)⎕STOP⊃⎕SI
 :EndIf
 :Trap DEBUG↓0
     ⎕IO←1
     (ns names)←2↑↓⍉↑⎕SE.Link.U.CurrentAplName¨,⊆files
     items←ns('.'U.JoinEach)names
     items←⊃⍣(1≥|≡files)⊢items
 :Else
     U.Resignal 1
 :EndTrap
