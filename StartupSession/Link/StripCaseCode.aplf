 StripCaseCode←{ ⍝ Change case coded filenames into plain filenames
     ⎕IO←0
     1<|≡⍵:∇¨⍵
     ~'-'∊⍵:⍵ ⍝ no case code?
     (p f e)←⎕NPARTS ⍵
     (n c)←2↑'-'(≠⊆⊢)f
     0∊c∊⎕D:⍵ ⍝ not a proper case code
     a←⌽,⍉2 2 2⊤⎕D⍳c∩⎕D
     p,e,⍨n U.Case⍨a↑⍨≢n
 }
